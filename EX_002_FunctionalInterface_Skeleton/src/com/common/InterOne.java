package com.common;

/**
 * 
 * @It is internally functional Interface, because it has only one abstract
 *     method
 * @It can have N no of static and default method no restriction and it is not
 *     compulsory to static and default methods
 *
 */
public interface InterOne {
	// default methods in interface are abstract
	public void m1();

}
